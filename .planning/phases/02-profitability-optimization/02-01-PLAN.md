---
phase: 02-profitability-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/analyticsService.js
  - src/ui/analytics.js
  - src/ui/index.html
  - src/ui/style.css
  - test/analyticsService.test.js
autonomous: true
requirements:
  - PROF-03

must_haves:
  truths:
    - "Analytics service returns byMarketRegime grouping (Oversold/Ranging/Overbought based on RSI at entry)"
    - "groupSummary includes profitFactor per bucket alongside existing win/loss/winRate/avgPnl"
    - "Analytics tab displays segmented performance tables for entry phase, session, and market regime"
    - "Each segmentation table shows Segment, Trades, Win Rate, Profit Factor, PnL, Avg PnL columns"
    - "Segments with fewer than 5 trades show a low-confidence indicator"
  artifacts:
    - path: "src/services/analyticsService.js"
      provides: "byMarketRegime grouping and profitFactor in groupSummary"
      exports: ["computeAnalytics", "groupSummary"]
    - path: "src/ui/analytics.js"
      provides: "Segmented performance rendering with sub-tabs"
    - path: "src/ui/index.html"
      provides: "HTML structure for segmented performance section"
  key_links:
    - from: "src/ui/analytics.js"
      to: "src/services/analyticsService.js"
      via: "/api/analytics endpoint returning segmented data"
      pattern: "byMarketRegime|byEntryPhase|bySession"
---

<objective>
Add segmented performance views to the Analytics tab showing win rate and profit factor broken down by entry phase (EARLY/MID/LATE), trading session (Asia/London/NY/Off-hours), and market regime (Oversold/Ranging/Overbought).

Purpose: Users need to understand which conditions produce the best trades. Segmented views reveal whether certain entry phases, times of day, or market regimes consistently outperform or underperform, enabling targeted threshold adjustments.

Output: Three segmentation tables in the Analytics tab with WR, PF, PnL metrics per segment, plus profitFactor added to all groupSummary buckets.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-profitability-optimization/02-CONTEXT.md
@.planning/phases/02-profitability-optimization/02-RESEARCH.md

@src/services/analyticsService.js
@src/ui/analytics.js
@src/ui/index.html
@src/ui/style.css
@test/analyticsService.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add byMarketRegime grouping and profitFactor to groupSummary</name>
  <files>
    src/services/analyticsService.js
    test/analyticsService.test.js
  </files>
  <action>
    **1. Extend `groupSummary()` to include `profitFactor` per bucket:**

    In the existing `groupSummary()` function, after computing `winRate` and `avgPnl`, add profit factor computation. Track `winPnl` and `lossPnl` separately per bucket during the accumulation loop:
    - For each trade, if `pnl > 0`, add to `winPnl`; if `pnl < 0`, add to `lossPnl`
    - `profitFactor = lossPnl !== 0 ? winPnl / Math.abs(lossPnl) : null`
    - Add `profitFactor` to the bucket result object

    **2. Add `regimeKeyFromTrade(trade)` function:**

    New function that classifies trades by RSI market regime using `trade.rsiAtEntry`:
    - RSI < 30: return `'Oversold'`
    - RSI >= 30 and RSI < 70: return `'Ranging'`
    - RSI >= 70: return `'Overbought'`
    - If `rsiAtEntry` is not a finite number: return `'unknown'`

    Export this function for testability.

    **3. Add `byMarketRegime` to `computeAnalytics()` return object:**

    Add after the existing `bySession` line:
    ```javascript
    byMarketRegime: groupSummary(closed, regimeKeyFromTrade),
    ```

    **4. Add tests in `test/analyticsService.test.js`:**

    - Test `regimeKeyFromTrade` with trades having RSI values in each regime (20, 50, 80) and missing/null RSI
    - Test that `groupSummary` now includes `profitFactor` in results
    - Test profitFactor: known trades with $10 win and -$5 loss should yield PF=2.0
    - Test profitFactor with only wins (should be null since no losses)
    - Test profitFactor with only losses (should be 0 since 0/|losses|)
    - Test that `computeAnalytics` output includes `byMarketRegime` key
  </action>
  <verify>
    <automated>cd /Users/elijahprince/Dev/polymarket-btc-5m-assistant && node --test test/analyticsService.test.js 2>&1 | tail -20</automated>
    <manual>Check that /api/analytics response includes byMarketRegime and profitFactor in groupings</manual>
  </verify>
  <done>
    - groupSummary includes profitFactor (winPnl / |lossPnl|) per bucket
    - regimeKeyFromTrade classifies by Oversold/Ranging/Overbought
    - computeAnalytics returns byMarketRegime
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add segmented performance UI to Analytics tab</name>
  <files>
    src/ui/index.html
    src/ui/analytics.js
    src/ui/style.css
  </files>
  <action>
    **1. Add HTML structure in `src/ui/index.html`:**

    In the `tab-analytics` div, AFTER the existing "Period Performance" card and BEFORE the "Advanced Metrics" card, add:

    ```html
    <!-- Segmented Performance -->
    <div class="card">
      <div class="card-title">Segmented Performance</div>
      <div class="period-tabs">
        <button class="seg-tab-btn active" data-seg="entryPhase">By Entry Phase</button>
        <button class="seg-tab-btn" data-seg="session">By Session</button>
        <button class="seg-tab-btn" data-seg="regime">By Market Regime</button>
      </div>
      <div id="segmented-table-container">
        <p class="muted-text">Loading segmented data...</p>
      </div>
    </div>
    ```

    **2. Add segmented performance rendering in `src/ui/analytics.js`:**

    After the existing period tab navigation code, add segmentation tab navigation:

    - Track `activeSegment` state (default: `'entryPhase'`)
    - On seg-tab-btn click, update `activeSegment`, toggle active class, render the selected segment
    - `renderSegmentedTable(data)` function:
      - Map `activeSegment` to analytics data key:
        - `'entryPhase'` -> `data.byEntryPhase`
        - `'session'` -> `data.bySession`
        - `'regime'` -> `data.byMarketRegime`
      - Build table with columns: Segment | Trades | Win Rate | Profit Factor | PnL | Avg PnL
      - For rows with fewer than 5 trades, add a `low-confidence` CSS class and a warning icon
      - Format: WR as percentage, PF to 2 decimal places, PnL with $ sign and color coding
      - Sort by PnL descending (most profitable segment first)
    - Call `renderSegmentedTable(cachedAnalytics)` when analytics data is loaded and when segment tab changes

    **3. Add styles in `src/ui/style.css`:**

    - `.seg-tab-btn` — Style identically to `.period-tab-btn` (reuse the existing pattern)
    - `.segmented-table` — Style identically to `.period-table`
    - `.low-confidence` — Slightly dimmed text (opacity 0.6) to indicate small sample size
    - Profit factor column: positive PF >= 1.0 gets green color, PF < 1.0 gets red color

    **Important:** Reuse the existing table styling from `.period-table`. Do NOT create significantly different visual patterns. The segmented tables should look consistent with the period performance tables.
  </action>
  <verify>
    <automated>grep -c "seg-tab-btn" src/ui/index.html</automated>
    <manual>Open the dashboard, navigate to Analytics tab, verify three segmentation sub-tabs render tables with WR and PF columns</manual>
  </verify>
  <done>
    - Segmented Performance card renders in Analytics tab with 3 sub-tabs
    - Tables show Segment, Trades, Win Rate, PF, PnL, Avg PnL
    - Low-confidence segments visually distinguished
    - Consistent styling with existing period tables
  </done>
</task>

</tasks>

<verification>
1. `npm test` passes with all existing + new tests
2. /api/analytics response includes `byMarketRegime` with Oversold/Ranging/Overbought buckets
3. All groupSummary results include `profitFactor` field
4. Analytics tab shows "Segmented Performance" card with 3 sub-tabs
5. Each segmentation table shows WR and PF columns with correct values
6. Segments with < 5 trades show low-confidence indicator
</verification>

<success_criteria>
- byMarketRegime grouping correctly classifies by RSI regime
- profitFactor included in all groupSummary output
- Three segmentation views rendered in Analytics tab UI
- Consistent visual design with existing period tables
- Zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-profitability-optimization/02-01-SUMMARY.md`
</output>
