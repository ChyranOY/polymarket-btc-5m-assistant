---
phase: 03-live-trading-hardening
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - src/ui/index.html
  - src/ui/script.js
  - src/ui/style.css
  - src/infrastructure/executors/LiveExecutor.js
  - src/application/TradingEngine.js
autonomous: true
requirements:
  - LIVE-01
  - LIVE-05
must_haves:
  truths:
    - "Dashboard shows order lifecycle state with colored indicator (SUBMITTED/PENDING/FILLED/MONITORING/EXITED)"
    - "Dashboard shows timestamps for each lifecycle transition"
    - "Dashboard shows sync status indicator (green/yellow/red) in Live mode"
    - "Dashboard has kill-switch override button with confirmation dialog"
    - "Kill-switch status is visible in dashboard with current daily PnL and limit"
    - "Fee-aware sizing is called by LiveExecutor with current fee rate"
  artifacts:
    - path: "src/ui/index.html"
      provides: "Dashboard markup for lifecycle card, sync indicator, kill-switch panel"
    - path: "src/ui/script.js"
      provides: "Polling and rendering for lifecycle state, sync status, kill-switch override"
    - path: "src/ui/style.css"
      provides: "Styled lifecycle states, sync indicator colors, kill-switch UI"
  key_links:
    - from: "src/ui/script.js"
      to: "src/ui/server.js"
      via: "script.js polls /api/status for lifecycle, reconciliation, kill-switch data"
      pattern: "/api/status|/api/kill-switch"
    - from: "src/infrastructure/executors/LiveExecutor.js"
      to: "src/domain/sizing.js"
      via: "LiveExecutor uses computeTradeSizeWithFees"
      pattern: "computeTradeSizeWithFees"
---

<objective>
Wire the lifecycle, reconciliation, and kill-switch features into the dashboard UI and complete the LiveExecutor integration with fee-aware sizing.

Purpose: LIVE-01 requires dashboard lifecycle display. LIVE-05 requires dashboard kill-switch override. This plan connects the domain/infra work from Plans 01-02 to the user-facing dashboard and finalizes the LiveExecutor integration.
Output: Dashboard showing lifecycle states, sync indicator, kill-switch controls. LiveExecutor using fee-aware sizing.
</objective>

<execution_context>
@/Users/elijahprince/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elijahprince/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-live-trading-hardening/03-CONTEXT.md
@.planning/phases/03-live-trading-hardening/03-01-SUMMARY.md
@.planning/phases/03-live-trading-hardening/03-02-SUMMARY.md
@src/ui/index.html
@src/ui/script.js
@src/ui/style.css
@src/ui/server.js
@src/infrastructure/executors/LiveExecutor.js
@src/application/TradingEngine.js
@src/domain/sizing.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate fee-aware sizing into LiveExecutor and wire lifecycle data to API</name>
  <files>src/infrastructure/executors/LiveExecutor.js, src/application/TradingEngine.js, src/ui/server.js</files>
  <action>
    Update LiveExecutor.openPosition() to use fee-aware sizing:
    - Import computeTradeSizeWithFees from domain/sizing.js
    - Before computing order size, get feeRateBps from this.feeService.getFeeRateBps(tokenID)
    - Use computeTradeSizeWithFees(collateral, this.config, feeRateBps) instead of raw Math.floor(usd / buyPrice)
    - Log fee deduction: "[live] Fee-adjusted size: $X (raw: $Y, fee: Z bps)"
    - If feeRateBps lookup fails, fall back to existing sizing (no fee deduction)

    Update server.js GET /api/status to include:
    - orderLifecycle: array of active order views from OrderManager.getAllOrderViews()
      Each view: { orderId, state, tokenID, side, price, size, fillSize, fillPrice, fillRatio, timestamps }
    - killSwitch: from tradingState.getKillSwitchStatus()
      { active, overrideActive, overrideCount, todayPnl, limit }
    - failureEvents: last 10 from LiveExecutor.getFailureEvents() (capped for API response size)
    - reconciliation: from LiveExecutor.getReconciliationStatus() (live mode only)

    Ensure these fields are included ONLY when relevant:
    - orderLifecycle: always included (empty array when no active orders)
    - killSwitch: always included (applies to both modes)
    - reconciliation: only in live mode
    - failureEvents: only in live mode
  </action>
  <verify>npm test passes. GET /api/status returns orderLifecycle, killSwitch, and reconciliation fields.</verify>
  <done>LiveExecutor uses fee-aware sizing. API exposes all Phase 3 data for dashboard consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Add lifecycle, sync indicator, and kill-switch UI to dashboard</name>
  <files>src/ui/index.html, src/ui/script.js, src/ui/style.css</files>
  <action>
    Update index.html — add UI elements in the Live mode status area:

    1. Order Lifecycle Card (visible in Live mode when orders exist):
       - Show current order state with colored badge:
         SUBMITTED (yellow), PENDING (yellow pulse), FILLED (green), MONITORING (blue), EXITED (gray), TIMED_OUT (orange), CANCELLED (gray), FAILED (red)
       - Show timestamps for each transition as compact timeline
       - Show fill details: fillSize/requestedSize = fillRatio%
       - If no active orders: show "No active orders"

    2. Sync Status Indicator (Live mode only):
       - Small colored dot in the Live status card header area:
         green dot + "In Sync" — reconciliation passing
         yellow dot + "Checking" — reconciliation in progress or recent order within grace window
         red dot + "Discrepancy" — mismatch detected, show details on hover/click
       - If discrepancy: show details below indicator (token IDs, qty differences, type)

    3. Kill-Switch Panel (both modes):
       - Show: "Daily PnL: -$X / -$Y limit"
       - Progress bar showing loss as percentage of limit (green -> yellow -> red as approaching limit)
       - When active: show "KILL-SWITCH ACTIVE" banner in red
       - Override button (visible only when kill-switch is active):
         - Button text: "Override Kill-Switch"
         - On click: confirmation dialog "Are you sure? Trading will resume but kill-switch can re-trigger. Override count: N"
         - On confirm: POST /api/kill-switch/override
         - After override: show "Overridden (N times)" in yellow
       - When not active: show status quietly (just the PnL / limit line)

    4. Failure Events (Live mode, collapsed by default):
       - Small section showing recent failure events count
       - Expandable to show last 5 events with type, error message, retry count, timestamp

    Update script.js:
    - In the polling loop, read new fields from /api/status response
    - Render order lifecycle card when orderLifecycle array has entries
    - Render sync indicator from reconciliation field
    - Render kill-switch panel from killSwitch field
    - Render failure events section from failureEvents field
    - Add click handler for kill-switch override button with confirmation dialog
    - Follow existing patterns: vanilla JS, no framework, DOM manipulation with getElementById/querySelector

    Update style.css:
    - Lifecycle state badges: colored rounded rectangles matching state colors above
    - Sync indicator: small 8px dots with appropriate colors, pulse animation for yellow
    - Kill-switch banner: full-width red banner when active, with override button styled as caution (yellow/orange)
    - Confirmation dialog: modal overlay with confirm/cancel buttons
    - PnL progress bar: thin bar under kill-switch info, green->yellow->red gradient
    - Follow existing dashboard style patterns (dark theme, consistent spacing, similar to existing status cards)
  </action>
  <verify>Dashboard displays lifecycle states, sync indicator, kill-switch panel. Override button shows confirmation dialog and calls API.</verify>
  <done>Dashboard fully displays all Phase 3 features: order lifecycle with colored states, sync indicator, kill-switch with override, failure events.</done>
</task>

</tasks>

<verification>
- Dashboard shows order lifecycle state for active orders with colored indicators
- Dashboard shows lifecycle transition timestamps
- Sync indicator is green when reconciliation passes, red when discrepancy detected
- Sync indicator only appears in Live mode
- Kill-switch panel shows daily PnL vs limit with progress bar
- Override button appears when kill-switch is active, requires confirmation
- Override calls API and updates UI to show override status
- Fee-aware sizing is used by LiveExecutor (log shows fee deduction)
- All tests pass (npm test)
</verification>

<success_criteria>
- Users can see the current state of every order in the dashboard (SUBMITTED through EXITED)
- Users can see if local tracking matches CLOB state (sync indicator)
- Users can see their daily PnL against the kill-switch limit
- Users can override the kill-switch with explicit confirmation
- LiveExecutor accounts for fees when sizing positions
- Dashboard updates in real-time (1.5s polling cycle)
- All existing dashboard functionality continues working
- All tests pass (npm test)
</success_criteria>

<output>
After completion, create `.planning/phases/03-live-trading-hardening/03-04-SUMMARY.md`
</output>
